## HTTP Methods

## GET

It's used to fetch a representation of the current state of the resource from the server. It's a kind of HTTP safe method which never effect any change in the sate of the resource.

```js
GET /about HTTP/1.0
```

Expected Response Codes

`200, (OK)`

`404, (Not Found)`

## 
