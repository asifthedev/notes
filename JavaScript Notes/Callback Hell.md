A function passed to another function as an argument is know as callback function.

```js
function getOrder(cart, function proceedToPayment(){});
```

Here `proceedToPayment()` is a callback function.